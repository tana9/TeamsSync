# アクセシビリティ確認

リリース候補をWindows上で次の順に確認する。

- Tab／Shift+Tabだけで、サインイン、チーム選択、入力方法、差分確認、同期、保存へ移動できる
- 貼り付け入力欄でEnterが改行として動作し、Ctrl+Enterだけが同期実行になる
- Narratorで対象チーム、同期モード、差分一覧、進捗、警告、主要ボタンの目的を読み上げる
- Windows表示倍率200%で操作部品や警告が欠けず、ウィンドウをスクロール・リサイズできる
- テーマは常にライト固定(ダーク自動切替は非対応)。Windows高コントラストモード時も文字と警告の判別ができる
- 操作種別が色だけでなく「追加」「削除」「エラー」などの文字でも判別できる
- Escapeによるキャンセルは同期実行中だけ有効で、確認ダイアログ操作を妨げない
